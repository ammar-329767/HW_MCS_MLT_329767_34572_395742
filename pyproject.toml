[build-system]
requires = ["setuptools>=70.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
requires-python = ">=3.10,<3.11"
dependencies = [
    "Flask",
    "numpy",
    "pandas",
    "scikit-learn",
]
